import type { Connection, ExecutedQuery, Transaction } from '@planetscale/database';
import type { Logger } from '../logger';
import type { MySqlDialect } from '../mysql-core/dialect';
import type { SelectedFieldsOrdered } from '../mysql-core/query-builders/select.types';
import { MySqlSession, MySqlTransaction, PreparedQuery, type PreparedQueryConfig, type QueryResultHKT } from '../mysql-core/session';
import { type Query } from '../sql';
export type PlanetScaleConnection = Connection;
export declare class PlanetScalePreparedQuery<T extends PreparedQueryConfig> extends PreparedQuery<T> {
    private client;
    private queryString;
    private params;
    private logger;
    private fields;
    private rawQuery;
    private query;
    constructor(client: PlanetScaleConnection | Transaction, queryString: string, params: unknown[], logger: Logger, fields: SelectedFieldsOrdered | undefined, name: string | undefined);
    execute(placeholderValues?: Record<string, unknown> | undefined): Promise<T['execute']>;
    all(placeholderValues?: Record<string, unknown> | undefined): Promise<T['all']>;
}
export interface PlanetscaleSessionOptions {
    logger?: Logger;
}
export declare class PlanetscaleSession extends MySqlSession {
    private baseClient;
    private options;
    private logger;
    private client;
    constructor(baseClient: PlanetScaleConnection, dialect: MySqlDialect, tx: Transaction | undefined, options?: PlanetscaleSessionOptions);
    prepareQuery<T extends PreparedQueryConfig = PreparedQueryConfig>(query: Query, fields: SelectedFieldsOrdered | undefined, name: string | undefined): PreparedQuery<T>;
    query(query: string, params: unknown[]): Promise<ExecutedQuery>;
    queryObjects(query: string, params: unknown[]): Promise<ExecutedQuery>;
    transaction<T>(transaction: (tx: MySqlTransaction<QueryResultHKT>) => Promise<T>): Promise<T>;
}
export declare class PlanetScaleTransaction extends MySqlTransaction<PlanetscaleQueryResultHKT> {
    transaction<T>(transaction: (tx: PlanetScaleTransaction) => Promise<T>): Promise<T>;
}
export interface PlanetscaleQueryResultHKT extends QueryResultHKT {
    type: ExecutedQuery;
}
//# sourceMappingURL=session.d.ts.map